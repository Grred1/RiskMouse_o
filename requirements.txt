# ── 核心依赖（必装）──────────────────────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
akshare>=1.12.0
pandas>=2.0.0
openai>=1.0.0
python-dotenv>=1.0.0
requests>=2.31.0
pillow>=10.0.0
numpy>=1.24.0

# ── 词云 + 中文分词（自选风控词云功能）──────────────────────────
wordcloud>=1.9.0
jieba>=0.42.0

# OCR 已移至前端 Tesseract.js，服务端无需额外依赖
python-jose>=3.5.0

# ── Coze SDK + LLM 消息类型 ──────────────────────────────────────
coze-coding-dev-sdk>=0.1.0
coze-coding-utils>=0.1.0
langchain-core>=0.3.0
