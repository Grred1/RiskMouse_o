# ── 核心依赖（必装）──────────────────────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
akshare>=1.12.0
pandas>=2.0.0
openai>=1.0.0
python-dotenv>=1.0.0
requests>=2.31.0
pillow>=10.0.0
numpy>=1.24.0

# ── 词云 + 中文分词（自选风控词云功能）──────────────────────────
wordcloud>=1.9.0
jieba>=0.42.0

# ── 截图 OCR（可选）─────────────────────────────────────────────
# Windows 用户安装前需先安装 Visual C++ 编译环境，安装失败可跳过
# 不安装则截图上传识别功能不可用，其他功能不受影响
# easyocr>=1.7.0
