## v1.10 — Coze 词云修复 + 股票不存在拦截
**改动**：
- **Coze 构建脚本补依赖**：`coze-preview-build.sh` 新增 `wordcloud` `jieba` `pillow` 三个包，修复部署后词云显示"暂无数据"的问题
- **自选股添加对不存在的股票代码拦截**：`watchlist.py` 添加股票时校验 `get_stock_name()` 返回为空则报 400 "股票代码不存在，请确认后重试"，阻止无效股票入库


## v1.9 — OCR 移至前端 Tesseract.js

**改动**：
- **蕾子最新提交**：截图识别从服务端 easyocr 改为前端 Tesseract.js
- **移除 `watchlist.py`**：删掉 `_extract_codes_from_image` + `parse_watchlist_image` 端点 + `import re`，共 53 行
- **新增 Tesseract.js CDN**：`index.html` 引入 `tesseract.js@5`，去掉 easyocr 相关依赖
- **`watchlist-core.js`**：OCR 逻辑从调后端 API 改为 `Tesseract.recognize()` 本地浏览器识别，状态提示改为 `🔍 首次使用需下载引擎`


## v1.8 — 缓存统一 + 清理 + RAG 优化

**改动**：
- **统一缓存**：SQLite watchlist_analysis 改为 filesystem，三种缓存归一（Agent / 日级快照 / 自选股分析全走 `read_cache`/`write_cache`），不再各自为政
- **新增 `core/cache/` 管理层**：
  - `cleanup.py` — TTL 清理：启动时自动删除 7 天前的 `news_full_*` / `sparkline_*` 日级快照
  - `rag.py` — 智能 RAG 索引：过滤噪音文件（跳过日级快照 / `macro_*` / `stock_names.json`）+ 按 mtime 增量加载 + 类封装 `CacheRAG`
  - `__init__.py` — 统一导出
- **向后兼容**：`core/cache_rag.py` 改为兼容层，委托 `cache/rag.py`，所有旧导入不受影响
- `cache_rag` 新增 `risk_interpret` / `risk_analyze` / `financial_combined` 等缓存类型的识别
- **main.py** 启动时新增 `cache_mgr.cleanup_cache()` 自动清理


## v1.7 — 宏观日历数据源修复 + Coze 真伪鉴定修通

**改动**：
- **修复财联社 API**：旧接口 `api/sw?app=CLS` 返回 405，改用 `nodeapi/telegraphList`，参数同步更新
- **修复股吧编码乱码**：东方财富返回 ISO-8859-1 但内容为 UTF-8，`guba.py` 三处爬虫加 `r.encoding = 'utf-8'`
- **修复新华社 RSS**：旧链接 404，增加备用 URL 自动回退
- **修复 Coze 真伪鉴定 `verify_news()`**：
  - `.env` 变量名不匹配修复（`SESSION_ID` → `COZE_SESSION_ID`）
  - JSON 解析兼容中文字段（`判定结果`、`置信度分数`），支持数字转文字置信度
- 删除流程中测试临时文件


## v1.6 — 合并蕾子功能（认证 + 情绪急救 + 风险教育 + 风险日记）

**改动**：
- **选择性合并**：从远端 origin/main 的 5 个 commit 中，只提取同事新增的功能代码，不引入已清理的旧结构
- **新增文件（直接复制）**：
  - `auth.py` — 用户注册/登录/JWT 认证模块
  - `core/skill_emotional.py` — 情绪急救工具（恐慌/贪婪检测 + 行为金融学引导）
  - `core/skill_risk_edu.py` — 风险教育工具（关键词匹配 + 知识库查询）
  - `backend/prompts/skills/` — 9 个 .md 知识库文件（止损/仓位/估值/宏观等）
  - `frontend/src/js/auth.js` — 前端登录/注册弹窗
  - `frontend/timeline.html` — 着陆页
- **选中合并（修改现有文件）**：
  - **db.py** — 新增用户表 + 风险日记表 + 用户 CRUD + `get_all_watchlist_stocks`（后台巡检用）
  - **core/config.py** — 新增 `load_skill_md()` 函数
  - **core/__init__.py** — 导出 skill 模块
  - **main.py** — 注册 auth 路由
  - **watchlist.py** — 加 auth 依赖注入 + 风险日记 API 端点
  - **mouse_agent.py** — 加意图识别(`_identify_intents`) + 工具调度(`_dispatch_tools`)，集成到 super_agent 对话
  - **super_agent.py/prompt** — 新增 tool_context + length_instruction 支持
  - 前端：`index.html`、`watchlist-core.js`、`macro.js`、`mouse-agent.js`、`common.css`、`mouse-agent.css`
- **特意排除**：不引入 `backend/prompts/*.txt` 旧 prompt、不引入 `core/coze_agent.py`（已被 `agents/coze_agent.py` 替代）
- 新增依赖：`python-jose` (JWT 认证)


## v1.5 — 数据接口统一管理层（core/data/）

**改动**：
- 新增 `core/data/` 统一数据接口层，集中管理所有数据源：
  - `data/akshare.py`：封装全部 17 个 AKShare 调用（主营构成/三表/涨停/龙虎榜/新闻/评论/热度/K线/宏观），统一错误处理
  - `data/guba.py`：封装股吧爬虫（个股帖子/大盘帖子/市场逻辑），消除 3 份重复代码
  - `data/news.py`：封装新闻源爬虫（财新/财联社/新华社），含聚合入口 `fetch_all_news`
- **删除散落重复代码**：macro.py 删除 5 个私有爬虫函数（共 162 行）；sentiment.py 删除重复 `_scrape_guba_posts`（32 行）；mouse_agent.py 删除重复 `_scrape_guba_posts`（32 行）
- **各 API 文件迁移**：
  - **finance.py**：4 处 AKShare 调用 → `data_akshare.*`
  - **sentiment.py**：8 处 AKShare 调用 + 1 处股吧爬虫 → `data_akshare.*` + `data_guba.*`
  - **watchlist.py**：3 处 AKShare 调用 → `data_akshare.*`
  - **macro.py**：`_fetch_all_sources` 委托 `data_news.fetch_all_news`
  - **mouse_agent.py**：4 处 AKShare 调用 + 3 处股吧爬虫 → `data_akshare.*` + `data_guba.*`


## v1.4 — Agent 层拆分 + API 分层清理

**改动**：
- 新增 `agents/analyze_watchlist_risk.py` Agent，封装自选风控 AI 解读逻辑
- 扩展 `agents/super_agent.py`（小老鼠 Agent），新增 `rag_context`/`usage_guide`/状态/屏幕输入，覆盖 chat 全线场景
- 扩展 `agents/prompts/super_agent.txt`，补充完整上下文模板
- **watchlist.py**：移除硬编码 prompt 加载 + `call_llm`/`cached_llm_call` → 委托 `run_agent("analyze_watchlist_risk", ...)`
- **macro.py**：移除内联 `RISK_FILTER_PROMPT`/`RISK_TIME_ANALYSIS_PROMPT` 两个大段 Prompt 字符串 + `call_llm` → 改用 `run_agent("filter_risk_events", ...)` 和 `run_agent("analyze_event_timing", ...)`
- **mouse_agent.py**：移除 `CHAT_PROMPT` 定义，chat 端点改调 `run_agent("super_agent", ...)`
- 清理后 API 层 `call_llm` 直接调用从 6 处降至 2 处（仅保留巡检内部子步骤）
- 分层干净：API 层负责 HTTP 编排，Agent 层负责 prompt + LLM + 缓存管理


## v1.3 — 宏观风险时间轴 + 月历视图

**改动**：
- 宏观日历改为多数据源实时采集（财新/财联社/新华社/股吧）
- LLM 智能过滤宏观风险事件 + 时间分析（判断持续时长、发生状态）
- 时间轴视图：过去/进行中/未来三栏统计 + 竖线时间线 + 事件详情面板
- 月历视图：网格月历 + 事件圆点（🔴🟡🟢）+ 点击查看当日事件详情
- 双视图切换（时间轴/月历），数据统一共享
- 语义去重 + 增量缓存 + 后台静默刷新


## v1.2 — 前端代码拆分 + 词云修复 + 手机端适配

**改动**：
- 前端 3 个大文件（753+467+641行）拆为 11 个单一功能模块
- 词云中文乱码：后端字体路径仅写了 Windows 路径，macOS 上找不到中文字体 → 新增智能字体查找模块，跨平台自动检测/下载
- 页面全尺寸响应式适配（768px/480px 断点）
- 手机端标题紧凑排版（左对齐标题 + 靠右市场时钟）
- 侧边栏全尺寸可折叠：桌面侧缘收起，手机左侧滑入面板（≤480px 宽100px），展开时内容右移不遮挡
- 各模块布局适配：三栏→单列、财报侧边栏→横向标签、风险挖掘上下堆叠、小老鼠缩小


## v1.1 — 目录结构扁平化 + 虚拟环境 + 启动脚本

**改动**：
- 移除冗余的 `business_analysis/` 层级，所有文件直接放在项目根目录
- 删除重复的 `backend/requirements.txt`，统一使用根目录的 `requirements.txt`
- 合并 `backend/.env.example` 和 `.env.example` 到根目录一份完整配置
- 创建 `.venv` 虚拟环境隔离 Python 依赖，不再污染系统环境
- 创建 `start.sh` 一键启动脚本


## v1.0 — 财务风险图表自适配

**改动**：7 个预定义图表在数据缺失时自动隐藏面板，不再显示"数据不可用"。

| # | 图表 | 位置 | 隐藏条件 |
|---|------|------|----------|
| 1 | 营收与利润趋势 | 增长分析 | 无年报数据 |
| 2 | 研发投入 | 增长分析 | `RESEARCH_EXPENSE` 全为空 |
| 3 | 费用率趋势 | 增长分析 | 三费字段全为空 |
| 4 | 资产扩张 | 增长分析 | `FIXED_ASSET` 全为空 |
| 5 | 盈利能力趋势 | 增长分析 | 财务摘要无年报数据 |
| 6 | 资产结构 | 健康分析 | 无年报数据 |
| 7 | 现金流 | 健康分析 | 无年报数据 |

**典型**：银行股(000001)无费用明细 → 隐藏 2、3；制造业(002081)全显示。
