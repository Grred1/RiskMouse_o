# ── 核心依赖（必装）──────────────────────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
akshare>=1.12.0
pandas>=2.0.0
openai>=1.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# ── 词云 + 中文分词（自选风控词云功能）──────────────────────────
wordcloud>=1.9.0
jieba>=0.42.0

# ── 截图 OCR（可选，不安装则图片上传识别不可用）─────────────────
easyocr>=1.7.0
pillow>=10.0.0
numpy>=1.24.0
