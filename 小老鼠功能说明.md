# 🐭 小老鼠 AI 助手 — 完整功能说明

## 一、整体架构

小老鼠是一个 **Web Component** `<mouse-assistant>`，前端自包含组件 + 后端独立 API 模块，通过轮询 + 定时任务驱动。

| 层级 | 关键文件 | 角色 |
|------|----------|------|
| **前端** | `frontend/src/js/mouse-agent.js` | Web Component，自包含 UI |
| **前端样式** | `frontend/src/styles/mouse-agent.css` | 角色动画 + 聊天/通知面板 |
| **后端 API** | `backend/app/api/mouse_agent.py` | 全部业务逻辑：巡检/鉴定/聊天/通知 |
| **Agent 层** | `backend/app/agents/super_agent.py` | 对话整合：RAG + 工具 + 文档上下文 |
| **工具** | `core/skill_emotional.py` | 情绪急救（恐慌/贪婪检测） |
| **工具** | `core/skill_risk_edu.py` | 风险教育（9 个知识库 .md） |
| **RAG** | `core/cache/rag.py` | 本地缓存知识库检索 |
| **文档** | `core/usage_doc.py` | 功能使用指南 |

---

## 二、核心工作模式

### 1. 定时深度巡检（每 10 分钟）

由 `_timer_loop()` 驱动，每次执行 `_deep_surf()`：

1. **宏观新闻抓取**：从财新/财联社/新华社抓取近 3 天财经新闻，用 `source:title` 去重（已分析过的自动跳过），LLM 判断是否有重要宏观事件需要提醒用户，有则同时推送通知和写入宏观日历
2. **自选股股吧扫描**：遍历用户所有自选股，爬取股吧帖子，LLM 筛选有实质性内容的帖子，调用 Coze Agent 做真伪鉴定
   - 判为虚假且高置信度 → ⚠️ 红色预警通知
   - 判为虚假但低置信度 → 🔍 待核实通知（建议人工核实）
   - 可信 → ✅ 静默通过

### 2. 空闲巡逻（每 2 分钟）

由 `_idle_patrol()` 执行：
- 遍历用户所有自选股
- 查阅 RAG 缓存知识库中的历史分析记录
- 爬取股吧最新帖子浏览

### 3. 对话交互

`chat()` 端点的完整链路：

```
用户问题
    │
    ├─→ 意图识别 (_identify_intents)
    │      ├─ 情绪检测 → 恐慌/贪婪 → 触发情绪急救
    │      └─ 风险教育匹配 → 触发知识库检索
    │
    ├─→ 工具调用 (_dispatch_tools)
    │      ├─ 情绪急救 (skill_emotional.py)
    │      └─ 风险教育 (skill_risk_edu.py)
    │
    ├─→ RAG 查询 (cache_rag.search_as_context)
    │
    ├─→ 使用文档查询 (usage_doc.search_usage)
    │
    └─→ super_agent 整合所有上下文 → LLM 回复
```

---

## 三、全部能力清单

| 能力 | 实现方式 | 触发时机 |
|------|----------|----------|
| **宏观新闻抓取** | `data_news.fetch_all_news()` 财新/财联社/新华社 + LLM 分析 | 每 10 分钟深度巡检（已分析过的自动跳过） |
| **股吧帖子爬取** | `guba.py` 爬虫 + LLM 筛选 | 深度巡检 + 空闲巡逻 |
| **新闻真伪鉴定** | `verify_news()` → Coze Agent | 深度巡检发现重要帖子时 |
| **自选股浏览** | 遍历自选股 + 爬股吧 | 空闲巡逻（每 2 分钟） |
| **RAG 知识库检索** | `cache_rag` 索引本地缓存 JSON | 对话时自动检索 |
| **情绪急救** | `skill_emotional.py` 恐慌/贪婪检测 + 行为金融学引导 | 检测到用户情绪化时 |
| **风险教育** | `skill_risk_edu.py` 关键词匹配 + 9 个 .md 知识库 | 检测到风险概念问题时 |
| **使用指南问答** | `usage_doc.py` 全功能文档关键词搜索 | 用户问"怎么用"时 |
| **邮件通知** | SMTP 发信（支持 QQ 邮箱等） | 产生风险通知时 |
| **通知推送** | 前端气泡徽标 + 通知面板 | 持续轮询新通知 |
| **深度聊天** | `super_agent` 整合 RAG + 工具 + 文档上下文 | 用户主动对话 |
| **实时状态展示** | 屏幕模拟终端 8 行滚动 | 所有动作实时更新 |
| **角色动画** | CSS 状态切换（idle/surfing/thinking/alert） | 状态变化时 |
| **拖拽移动** | 鼠标拖拽定位组件 | 用户交互 |
| **展开/收起聊天** | 浮窗 ↔ 全屏模式切换 | 用户交互 |

---

## 四、后端 API 端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/agent/status` | GET | 获取当前状态：巡检状态、屏幕日志、最近 5 条通知、定时器是否运行 |
| `/api/agent/surf` | POST | 异步触发深度巡检（不等待完成） |
| `/api/agent/surf-sync` | GET | 同步触发深度巡检，等待完成并返回结果 |
| `/api/agent/timer-start` | GET | 启动定时任务（每 10 分钟） |
| `/api/agent/timer-stop` | GET | 停止定时任务 |
| `/api/agent/chat` | POST | 对话提问，返回 AI 回复 + 识别到的意图 |
| `/api/agent/notifications` | GET | 获取最近 10 条通知 |
| `/api/agent/notifications/clear` | POST | 清空所有通知 |
| `/api/agent/email-config` | GET/POST | 获取/设置邮件通知配置 |
| `/api/agent/rag-info` | GET | 获取 RAG 知识库统计（缓存股票数量等） |

---

## 五、前端组件特性

| 特性 | 说明 |
|------|------|
| 自包含 Web Component | `<mouse-assistant>` 标签即用，不与页面其他元素冲突 |
| 状态动画 | idle（待命）/ surfing（冲浪）/ thinking（思考）/ alert（告警）四种 CSS 状态 |
| 小屏幕 | 终端风格，滚动显示 8 行实时日志 |
| 通知气泡 | 角色头顶显示未读数（≥99 显示 99+） |
| 通知面板 | 点击气泡展开，按时间倒序排列，支持清空 |
| 聊天窗口 | 浮窗 / 全屏两种模式可切换 |
| 拖拽 | 可拖到屏幕任意位置，位置持久化 |
| 首次自动冲浪 | 页面加载 5 秒后自动触发第一次深度巡检 |
| 3 秒轮询 | 实时拉取后端状态、屏幕日志、新通知 |

---

## 六、风险教育知识库（9 个）

存放于 `backend/prompts/skills/`：

| 文件 | 主题 | 触发关键词 |
|------|------|-----------|
| `risk_edu_stop_loss.md` | 止损 | 止损、割肉、亏了、被套 |
| `risk_edu_position.md` | 仓位管理 | 仓位、满仓、空仓、加仓 |
| `risk_edu_valuation.md` | 估值 | 估值、PE、PB、贵不贵、泡沫 |
| `risk_edu_macro.md` | 宏观 | 宏观、降息、加息、通胀、CPI、PMI |
| `risk_edu_emotional.md` | 情绪 | 恐慌、贪婪、追涨杀跌、焦虑 |
| `risk_edu_liquidity.md` | 流动性 | 流动性、成交量、换手率、放量、缩量 |
| `risk_edu_diversification.md` | 分散投资 | 分散、集中、鸡蛋、篮子、配置 |
| `risk_edu_market_cycle.md` | 市场周期 | 周期、牛熊、底部、顶部 |
| `risk_edu_dividend.md` | 分红 | 分红、股息、派息 |

---

## 七、触发方式总结

| 触发方式 | 周期 | 行为 |
|----------|------|------|
| **页面加载 5s 后** | 一次性 | 首次深度巡检 |
| **空闲巡逻** | 每 2 分钟 | 浏览自选股股吧 |
| **深度巡检** | 每 10 分钟 | 宏观 + 热门股吧 + 真伪鉴定 |
| **用户点击角色** | 手动 | 打开聊天窗口 |
| **用户点击气泡** | 手动 | 展开通知面板 |
| **API 调用 /surf** | 手动 | 立即触发深度巡检 |
