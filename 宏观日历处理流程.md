# 宏观风险日历 — 完整处理流程

## 整体架构

```
[用户操作] → 前端 API 调用 → 后端 API 入口 → 数据采集 → 聚类 → LLM 过滤 → LLM 时间分析 → 合并 → 缓存 → 前端渲染
```

---

## 一、前端触发（macro.js）

### 入口函数：`fetchMacroRiskTimeline(refresh, background)`

| 调用方式 | 触发场景 | 前端行为 |
|---------|---------|---------|
| `refresh=false` | 页面首次加载 | 尝试读缓存，无缓存则自动触发后台采集 |
| `refresh=true&background=true` | 用户点"刷新" | 立即返回旧缓存（如有），后台静默更新 |

### 三种关键响应状态

| `cache_status` | 前端处理 |
|---------------|---------|
| `warm` | 直接渲染视图 |
| `background_updating` | 渲染返回的旧缓存 + 启动轮询等待新数据 |
| `background_started` | 显示 loading + 轮询等待 |

### 轮询机制

```
开始轮询
  └─ 延迟 2 秒
      └─ 每 5 秒请求一次 refresh=false
          ├─ cache_status === "warm" → 渲染新数据，停止轮询
          └─ 其他 → 继续轮询
              └─ 超过 60 次（5 分钟）→ 显示"数据加载超时"
```

---

## 二、后端 API 入口

### 路由：`GET /api/macro/risk-timeline`

**文件**: [macro.py](file:///d:/Project/Fengkong/backend/app/api/macro.py)

### 请求参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `refresh` | bool | false | 是否强制刷新 |
| `background` | bool | false | 后台静默更新 |
| `days` | int | 30 | 采集最近 N 天数据 |

### 请求处理流程

```
refresh=false
  ├─ 有缓存（warm）→ 直接返回
  └─ 无缓存 → _start_background_update() → 返回 background_started + 触发后台

refresh=true 或 background=true → _start_background_update()
  ├─ 有缓存 → 返回 background_updating + 旧缓存 + 后台线程更新
  └─ 无缓存 → 返回 background_started + 后台线程更新

refresh=true（同步，极少使用）
  └─ 完整链路同步执行，等 1-2 分钟返回结果
```

### `_start_background_update()` 后台线程

```python
def _start_background_update(cache_key, days):
    读取旧缓存（如有）
    启动后台线程：
        1. 读取持久事件库
        2. 数据采集
        3. 聚类
        4. LLM 过滤
        5. LLM 时间分析
        6. 事件合并
        7. 写持久事件库
        8. 写响应缓存
    返回旧缓存 / background_started
```

---

## 三、数据采集

### 方法：`_fetch_all_sources(days=30)`

**文件**: [news.py](file:///d:/Project/Fengkong/backend/app/core/data/news.py)

采集 4 路数据源，返回 `(official, social)` 两组新闻：

| 来源 | 方法 | 采集范围 | 归属 |
|------|------|---------|------|
| **财新网** | `fetch_caixin_news(3)` | 3 页，每页 100 条 | official |
| **财联社** | `fetch_cls_news(30)` | 最近 30 天电报 | official |
| **网站扫描** | `fetch_web_scan()` | 通用新闻站点 | official |
| **股吧热帖** | `fetch_hot_posts()` | 东方财富股吧 | social |

**输出**: 约 200-300 条原始新闻条目，每条包含 `{title, summary, url, date, source}`

---

## 四、新闻聚类

### 方法：`_cluster_news(official, social)`

**文件**: [news_cluster.py](file:///d:/Project/Fengkong/backend/app/core/cache/news_cluster.py)

### 算法

```
Jaccard 相似度（基于字符交集/并集）
相似度阈值：0.28
```

### 流程

```
200-300 条原始新闻
        │
        ▼
  遍历每条新闻，与已有聚类事件逐一比对
        │
        ├─ 相似度 ≥ 0.28 → 合并到现有事件
        │     ├─ 追加来源 URL
        │     ├─ 合并来源列表
        │     ├─ 更新日期范围（first_date / last_date）
        │     └─ hit_count +1
        │
        └─ 相似度 < 0.28 → 创建新事件聚类
```

### 聚类事件结构

```json
{
  "id": "a1b2c3d4e5f6",
  "title": "中东冲突致运费飙升",
  "summary": "中东冲突导致运费飙升...",
  "urls": [
    {"url": "...", "source": "caixin", "date": "2026-05-28"},
    {"url": "...", "source": "cls", "date": "2026-05-28"}
  ],
  "sources": ["caixin", "cls"],
  "first_date": "2026-05-27",
  "last_date": "2026-05-28",
  "hit_count": 3
}
```

**输出**: 约 40-80 个独立事件

---

## 五、LLM 风险过滤

### 方法：`_filter_risk_events(clusters)`

**Agent**: [filter_risk_events.py](file:///d:/Project/Fengkong/backend/app/agents/filter_risk_events.py)
**Prompt**: [filter_risk_events.txt](file:///d:/Project/Fengkong/backend/app/agents/prompts/filter_risk_events.txt)

### 并行策略

```
每批 20 条事件，5 线程并行
LLM 调用次数 ≈ ceil(N / 20) × 1
例如 60 个事件 → 3 次 LLM 调用
```

### LLM 输入格式

```
[2026-05-28] [caixin] 中东冲突致运费飙升
摘要: 中东冲突导致运费飙升...
链接: https://...

[2026-05-28] [cls] 中东局势紧张推高航运成本
摘要: 中东局势...
链接: https://...
```

### LLM 输出（每条事件）

```json
{
  "keep": true,
  "title": "中东冲突致运费飙升",
  "summary": "中东冲突导致运费飙升...",
  "date": "2026-05-28",
  "url": "https://...",
  "source": "caixin",
  "category": "大宗商品",
  "risk_level": "高"
}
```

### 过滤原则

| 保留 | 丢弃 |
|------|------|
| 地缘政治事件（战争、制裁、谈判） | 企业级新闻（某公司产品发布） |
| 货币政策（利率、汇率、通胀） | 娱乐/八卦/社会新闻 |
| 经济数据（GDP、CPI、PMI） | 日常股市波动 |
| 大宗商品异动（原油、矿产） | 行业常规新闻 |
| 宏观监管政策 | 地方性小事件 |

**输出**: 约 20-40 条保留事件

---

## 六、LLM 时间分析

### 方法：`_analyze_event_times(events)`

**Agent**: [analyze_event_timing.py](file:///d:/Project/Fengkong/backend/app/agents/analyze_event_timing.py)
**Prompt**: [analyze_event_timing.txt](file:///d:/Project/Fengkong/backend/app/agents/prompts/analyze_event_timing.txt)

### 并行策略

```
每批 10 条事件，5 线程并行
LLM 调用次数 ≈ ceil(N / 10) × 1
例如 30 个事件 → 3 次 LLM 调用
```

### LLM 输入

```json
{
  "current_date": "2026-05-28",
  "events_data": "- 报道日期: 2026-05-28\n"
                 "  标题: 中东冲突致运费飙升\n"
                 "  摘要: ...\n"
                 "  分类: 大宗商品"
}
```

### LLM 输出（每条事件，补充时间维度）

```json
{
  "first_occurrence": "2026-05-28",
  "duration": "中期影响",
  "duration_days": 90,
  "time_status": "进行中",
  "reasoning": "冲突已持续数周，航运成本影响预计持续数月"
}
```

### 时间状态规则

| `time_status` | 判断逻辑 | 前端展示 |
|--------------|---------|---------|
| **已结束** | `estimated_end < today` 或 LLM 判定结束 | 归入 past 时间轴 |
| **进行中** | `first_occurrence ≤ today ≤ estimated_end` | 归入 current 时间轴 |
| **预期发生** | `first_occurrence > today` | 归入 future 时间轴 |

其中 `estimated_end = first_occurrence + duration_days`

---

## 七、事件合并

### 方法：`_merge_events(existing, new)`

### 输入

```
新分析结果（20-40 条）
    +
persistent 缓存历史事件（累计增长）
```

### 去重策略

```
1. 精确去重：按标题字符串完全匹配
2. 语义去重：Jaccard 相似度 ≥ 0.6 → 合并
   合并时：合并 sources 列表，保留最新 URL
3. 按 first_occurrence 倒序排列
```

**输出**: 合并后的事件列表，存入 persistent 缓存

---

## 八、时间线分类

### 方法：`_classify_timeline_events(events)`

将合并后的事件分为 4 组：

```json
{
  "past": {
    "events": [...],
    "stats": {"total": 68, "high": 15, "medium": 30, "low": 23}
  },
  "current": {
    "events": [...],
    "stats": {...}
  },
  "future": {
    "events": [...],
    "stats": {...}
  },
  "by_risk_level": {
    "high": [...],
    "medium": [...],
    "low": [...]
  }
}
```

每条事件同时写入 `estimated_end` 供前端展示：

```
estimated_end = first_occurrence + duration_days
```

---

## 九、缓存写入

### 响应缓存

```
key:  "macro_risk_timeline_{days}d"    →  macro_macro_risk_timeline_30d.json
用途: API 直接返回给前端
TTL:  永久（直到下次刷新覆盖）
```

### 持久事件库

```
key:  "macro_risk_timeline_persistent"  →  macro_macro_risk_timeline_persistent.json
用途: 跨刷新保留历史事件，增量积累
TTL:  永久（只增不删）
```

### 缓存文件内容

```json
{
  "timeline": { ... },
  "stats": { "total": 100, "raw_count": 250 },
  "fetched_at": "2026-05-28 14:30:00"
}
```

---

## 十、前端渲染

### 入口：`renderMacroView()`

### 数据流转

```
收到数据
    │
    ├─ 按 time_status 过滤（all / past / current / future）
    ├─ 按 risk_level 过滤（all / 高 / 中 / 低）
    │
    ├─ 时间轴视图（timeline）
    │   ├─ 排序（按 first_occurrence 倒序）
    │   ├─ 左侧时间线（圆点颜色=风险等级，圆点样式=时间状态）
    │   │   ├─ 已结束 → 灰色圆点
    │   │   ├─ 进行中 → 亮色圆点
    │   │   └─ 预期发生 → 虚线圆点
    │   └─ 右侧详情面板（点击事件后显示）
    │
    └─ 月历视图（calendar）
        ├─ 按月生成网格
        ├─ 事件按日期填入对应格子
        └─ 红/黄/绿标记风险等级
```

### 前端过滤状态

```javascript
filterRisk  // "all" | "高" | "中" | "低"
filterTime  // "all" | "past" | "current" | "future"
```

---

## 时序总览

```
用户操作                          前端                   后端（主线程）             后端（后台线程）
    │                              │                        │                         │
    ├── 打开页面 ──→ fetch() ──→ 无缓存 ──→ _start_background_update()
    │                              │                        │                    ┌─────┴─────┐
    │                              │                  返回 background_started    │ 采集 10-20s│
    │                              │                        │                    │ 聚类 <1s   │
    │                        显示 loading                   │                    │ LLM过滤 30s│
    │                        启动轮询 5s                    │                    │ LLM时间 30s│
    │                              │                        │                    │ 合并+写入  │
    │                              │                        │                    └────────────┘
    ├── 轮询第 1 次 ──→ fetch() ──→ 无缓存 ──→ 继续等               │
    │                              │                        │                   后台写入完成
    ├── 轮询第 2 次 ──→ fetch() ──→ warm ──→ 返回数据               │
    │                              │                        │                         │
    │                         渲染视图                     （结束）                  （结束）
    │                              │
    ├── 点刷新 ──→ fetch(true, true)
    │                              │                        │
    │                    返回 background_updating + 旧缓存    │                    启动新后台
    │                    立即渲染旧视图                      │                      （同上）
    │                    启动轮询                            │
    │                              │                        │
    └── 轮询到 warm ──→ 替换视图                   （结束）
```

### 耗时估算

| 环节 | 耗时 | 说明 |
|------|------|------|
| 数据采集 | 10-20s | 4 个源串行爬取，含请求间隔 |
| 新闻聚类 | <1s | 内存 Jaccard 计算 |
| LLM 风险过滤 | 30-60s | 5 线程并行，按 20 条/批 |
| LLM 时间分析 | 30-60s | 5 线程并行，按 10 条/批 |
| 合计 | 约 1-2 分钟 | 主要耗时在 LLM 调用 |

### 缓存状态转换图

```
无缓存 ──→ background_started ──→（后台完成）──→ warm
无缓存 ──→ background_started ──→（后台完成）──→ warm
有缓存 ──→ background_updating ──→（后台完成）──→ warm
```

---

## 关键文件索引

| 文件 | 职责 |
|------|------|
| [frontend/src/js/macro.js](file:///d:/Project/Fengkong/frontend/src/js/macro.js) | 前端页面逻辑、渲染、轮询 |
| [backend/app/api/macro.py](file:///d:/Project/Fengkong/backend/app/api/macro.py) | 后端 API、缓存管理、编排流程 |
| [backend/app/core/data/news.py](file:///d:/Project/Fengkong/backend/app/core/data/news.py) | 多源新闻数据采集 |
| [backend/app/core/cache/news_cluster.py](file:///d:/Project/Fengkong/backend/app/core/cache/news_cluster.py) | 新闻聚类引擎 |
| [backend/app/core/cache/similarity.py](file:///d:/Project/Fengkong/backend/app/core/cache/similarity.py) | Jaccard 相似度计算 |
| [backend/app/agents/filter_risk_events.py](file:///d:/Project/Fengkong/backend/app/agents/filter_risk_events.py) | LLM 风险过滤 Agent |
| [backend/app/agents/analyze_event_timing.py](file:///d:/Project/Fengkong/backend/app/agents/analyze_event_timing.py) | LLM 时间分析 Agent |
| [backend/app/agents/prompts/filter_risk_events.txt](file:///d:/Project/Fengkong/backend/app/agents/prompts/filter_risk_events.txt) | 过滤 prompt |
| [backend/app/agents/prompts/analyze_event_timing.txt](file:///d:/Project/Fengkong/backend/app/agents/prompts/analyze_event_timing.txt) | 时间分析 prompt |
