# 架构规范化方案

## 核心原则

当前问题：Agent 散布在 5 个 API 文件中（finance.py、sentiment.py、macro.py、mouse_agent.py、watchlist.py），每个都是"拼 prompt → call_llm() → 缓存"三板斧，代码风格不一，互相不知道彼此存在。

方案目标：**统一一个 Agent 模块，所有 Agent = 配置（prompt + tools + cache），不再散落各处。**

---

## 一、统一 Agent 模块

### 现状（散落各处的 Agent）

```
finance.py                     sentiment.py                  macro.py
  ├─ PROMPT (硬编码)             ├─ PROMPT (硬编码)             ├─ RISK_FILTER_PROMPT
  ├─ call_llm()                 ├─ call_llm()                 ├─ RISK_TIME_ANALYSIS_PROMPT
  ├─ cached_llm_call()          ├─ cached_llm_call()          ├─ call_llm() x 2
  └─ 自己拼数据                   └─ 自己拼数据                   └─ 自己拼数据

mouse_agent.py                 watchlist.py                  coze_agent.py
  ├─ CHAT_PROMPT                 ├─ 简单 LLM 调用              ├─ call_coze_agent()
  ├─ NEWS_FILTER_PROMPT          └─ 无独立 prompt              └─ verify_news()
  ├─ _surf_macro() (硬编码)
  └─ _surf_guba_verify() (硬编码)
```

### 方案：Agent 目录，每个 Agent 一个文件

新增 `backend/app/agents/` 目录，每个 Agent 一个 `.py` 文件。

```
backend/app/agents/
├── __init__.py             ← 注册表 + Agent 基类 + list/run 入口
├── base.py                 ← Agent 基类定义
├── analyze_zygc.py         ← 主营构成分析
├── analyze_financial.py    ← 财报健康度 + 成长性分析
├── analyze_sentiment.py    ← 舆论情绪分析
├── filter_risk_events.py   ← 宏观风险事件过滤
├── analyze_event_timing.py ← 宏观事件时间分析
└── super_agent.py          ← 小老鼠（超级 Agent，可调用其他所有 Agent）
```

#### Agent 基类

```python
# backend/app/agents/base.py

from __future__ import annotations
from dataclasses import dataclass, field
from typing import Callable, Optional

@dataclass
class Agent:
    name: str                          # 唯一标识，如 "analyze_zygc"
    description: str                   # 自然语言描述
    category: str                      # 分组: finance / sentiment / macro / super
    prompt_template: str = ""          # 提示词模板
    input_keys: list[str] = field(default_factory=list)
    cache_key_pattern: str = ""        # 如 "finance:zygc_analysis:{code}"
    cache_ttl: int = 86400
    max_tokens: int = 600
    temperature: float = 0.3
    output_parser: Optional[Callable] = None

    def run(self, inputs: dict) -> str:
        """查缓存 → 拼 prompt → call_llm → 缓存 → 返回"""
        ...
```

#### 注册表

```python
# backend/app/agents/__init__.py

from .base import Agent
from .analyze_zygc import agent as analyze_zygc
from .analyze_financial import agent as analyze_financial
from .analyze_sentiment import agent as analyze_sentiment
# ...

AGENT_REGISTRY: list[Agent] = [
    analyze_zygc,
    analyze_financial,
    analyze_sentiment,
    # ...
]

def list_agents(category: str = "") -> list[dict]: ...
def run_agent(name: str, inputs: dict) -> str: ...
```

#### 单个 Agent 文件示例

```python
# backend/app/agents/analyze_zygc.py

from .base import Agent

agent = Agent(
    name="analyze_zygc",
    description="分析股票的主营构成，判断业务稳定性和收入集中度风险",
    category="finance",
    prompt_template="""你是一位财务分析师。请分析以下主营构成数据...

股票: {name}({code})
主营构成数据: {zygc_data}

输出 JSON：{{"concentration": "高/中/低", ...}}""",
    input_keys=["code", "name", "zygc_data"],
    cache_key_pattern="finance:zygc_analysis:{code}",
    cache_ttl=86400,
    max_tokens=500,
)
```

每个 Agent 文件只做一件事：**声明配置**。如果未来某个 Agent 需要更复杂的逻辑（多步骤推理、工具调用），直接在它自己的文件里加方法即可，不影响其他 Agent。`base.py` 也可以继续升级（比如加入默认的 tool 调用能力），子 Agent 继承即可。

#### 各 Agent 文件对照

| 文件 | 目前所在位置 | 配置复杂度 |
|------|-------------|-----------|
| `analyze_zygc.py` | finance.py (3 处 call_llm 之一) | prompt + inputs |
| `analyze_financial.py` | finance.py (另 2 处) | prompt + inputs |
| `analyze_sentiment.py` | sentiment.py (2 处) | prompt + inputs |
| `filter_risk_events.py` | macro.py (过滤步骤) | prompt + output_parser |
| `analyze_event_timing.py` | macro.py (时间分析步骤) | prompt + output_parser |
| `super_agent.py` | mouse_agent.py (chat + surf) | prompt + 可调其他 Agent |

---

## 二、统一缓存层

新增 `backend/app/core/cache_manager.py`。

### 核心接口

```python
class CacheManager:
    REGISTRY = {
        "finance:zygc:{code}":         {"ttl": 3600, "desc": "主营构成原始数据"},
        "finance:financial:{code}":    {"ttl": 3600, "desc": "财报原始数据"},
        "finance:zygc_analysis:{code}": {"ttl": 86400, "desc": "AI 主营构成分析"},
        "sentiment:pool":              {"ttl": 600, "desc": "风险股池"},
        "sentiment:guba_analysis:{code}": {"ttl": 600, "desc": "AI 舆情分析"},
        "macro:timeline":              {"ttl": 300, "desc": "宏观风险时间轴"},
    }

    @staticmethod
    def get(key: str) -> Optional[dict]:
        """读缓存，自动检查 TTL"""

    @staticmethod
    def set(key: str, data: dict, ttl: int = 0):
        """写缓存，记录时间戳"""

    @staticmethod
    def summary() -> list[dict]:
        """返回所有有效缓存的概览（供 Agent 发现）：[{key, ttl_remaining, desc}]"""

    @staticmethod
    def clear_expired() -> int:
        """清理过期缓存"""
```

现有 `read_cache` / `write_cache` 保留作为内部实现，新增接口在其上包装。

---

## 三、统一 Tool 层

有些操作不需要 LLM，只需要调用 API 获取数据（如 `GET /api/zygc?code=002081`）。统一注册为 Tool。

```python
@dataclass
class Tool:
    name: str
    description: str
    category: str
    parameters: list[dict]
    endpoint: str                    # 对应 API 路径
    cache_key_pattern: str = ""      # 对应缓存 key（如果有）


TOOL_REGISTRY: list[Tool] = [
    Tool("get_financial_data", "获取财报原始数据", "finance",
         [{"name": "code", "type": "string", "required": True}],
         "/api/financial", "finance:financial:{code}"),
    Tool("get_zygc_data", "获取主营构成原始数据", "finance",
         [{"name": "code", "type": "string", "required": True}],
         "/api/zygc", "finance:zygc:{code}"),
    Tool("get_risk_pool", "获取热门风险股池", "sentiment",
         [],
         "/api/risk/pool", "sentiment:pool"),
    Tool("get_macro_timeline", "获取宏观风险时间轴", "macro",
         [],
         "/api/macro/risk-timeline", "macro:timeline"),
    Tool("get_watchlist", "获取自选股列表", "watchlist",
         [],
         "/api/watchlist/list"),
]
```

---

## 四、Super Agent（小老鼠）升级

### 当前

```
用户提问 → 拼固定 prompt → call_llm() → 返回
```

### 升级后

```
用户提问
    ↓
查询 cache_manager.summary()    ← 知道缓存里有什么
查询 agents.list_agents()       ← 知道自己能调什么 Agent
查询 tool_registry.list_tools() ← 知道自己能调什么 API
    ↓
拼 SUPER_AGENT_PROMPT（含 agent_list + cache_summary）
    ↓
call_llm() → 决定要不要调 Agent / 查缓存
    ↓
执行 Agent.run() / Tool 调用
    ↓
拼回答 → 返回
```

小老鼠不再硬编码任何分析逻辑，所有能力来自注册表，**新加一个 Agent 一行代码，小老鼠自动知道**。

---

## 五、总结

| 模块 | 新增/改动 | 职责 |
|------|----------|------|
| `agents/` 目录 | **新增** | 所有 Agent 一个文件一个，可独立扩展 |
| `agents/base.py` | **新增** | Agent 基类（run/缓存/LLM 调用） |
| `agents/__init__.py` | **新增** | 注册表 + list/run 统一入口 |
| `agents/analyze_zygc.py` | **新增** | 主营构成分析 |
| `agents/analyze_financial.py` | **新增** | 财报健康度+成长性分析 |
| `agents/analyze_sentiment.py` | **新增** | 舆论情绪分析 |
| `agents/filter_risk_events.py` | **新增** | 宏观风险事件过滤 |
| `agents/analyze_event_timing.py` | **新增** | 宏观事件时间分析 |
| `agents/super_agent.py` | **新增** | 小老鼠（可调其他所有 Agent）|
| `cache_manager.py` | **新增** | 缓存统一管理 + TTL + 发现 |
| `api_response.py` | **新增** | 统一响应格式 |

| 改动的文件 | 改动内容 |
|-----------|----------|
| `finance.py` | 去掉 prompt 和 call_llm，改为调用 agents.run_agent() |
| `sentiment.py` | 同上 |
| `macro.py` | 同上 |
| `mouse_agent.py` | chat 使用 agents.list_agents() 动态构建能力，去掉硬编码 |
| `config.py` | read_cache/write_cache 保留作为 cache_manager 的内部实现 |
| `cache_rag.py` | 可通过 agents 注册表精确索引，不再猜文件内容 |

**关键收益**：
1. **新增一个 Agent = 注册一行**，不用改任何其他文件
2. **Super Agent 自动获得新能力**，不需要改 prompt
3. **所有缓存对 Agent 透明**，不再需要 cache_rag 猜文件内容
4. **原有 API 端点不变**，前端不感知后端重构
